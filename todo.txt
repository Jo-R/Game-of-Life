- be able to reset to how I started (copy intial state and then check in create board if !undefined and copy it if its there??)
- be able to move one step at a time (either from the off or when one is running)
- keyboard naviagtion
- use css grid instead of table?
- make it so can have it wrap off the edges
- customisable grid size?
- show generation number
- ?animate the unchecking of the boxes (tried :not(:checked) but it didn't like that)

